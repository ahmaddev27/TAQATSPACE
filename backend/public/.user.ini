; Raise PHP upload limits so chat attachments (capped at 10 MB by the app) are
; accepted. PHP defaults (~2 MB upload / 8 MB post) would otherwise reject them.
; PHP-FPM/CGI (cPanel) reads .user.ini from the document root.
upload_max_filesize = 12M
post_max_size = 13M
