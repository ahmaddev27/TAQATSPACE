NEXT_PUBLIC_API_URL=http://localhost:8000/api

# Mapbox GL public token (https://account.mapbox.com/access-tokens/).
# Maps fall back to a static placeholder when this is empty.
NEXT_PUBLIC_MAPBOX_TOKEN=
